/**
 * Array 예제1.js
 * JS의 배열은 연관 배열이 아니므로 문자열을 인덱스로 사용해 배열 요소에 접근 불가
 * 하지만 음수가 아닌 정수를 인덱스롤 사용해 접근
 * arr["text"] > php가 제공
 * 
 */
alp("123dgagdfa3r13qegaega3")


function alp(str1){
    let a = 'A'
    for(let i in str1){
        console.log(str1[i])
    }
}