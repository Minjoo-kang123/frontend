/**
 * 구조체 => zmffotm
 * 구조체 : 사용자가 만드는 데이터타입
 * 각 사용자가 사용하기 편하도록 일련의 데이터를 묶어서 한다
 */

struct Stack{
    int []st
}

/**
 * 객체 생성 시 -> 변수는 객체마다 만들어진다
 *              -> 매서드는 하나만 만들고 (this.객체주소를 담아서 보낸다.)
 * c에서는 구조체 그리고 타입스크립트에선 타입니다.
 */